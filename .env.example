# Supabase (required for remote saved_watches; app runs with placeholders if unset)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional — when "true", enables in-memory analytics buffer + DevPanel (dev only)
# NEXT_PUBLIC_TEST_MODE=true
